<% content_for :title, "Trump tweets" %>
<%= render DatatableFilterComponent.new(turbo_frame_id: :rows2, page_count: [10, 25, 50, 100], search: true) %>
<turbo-frame id="rows2">
  <turbo-frame id="rows" src="<%= rows_trump_tweets_path(@filters) %>">
  loading...
  </turbo-frame>
</turbo-frame>