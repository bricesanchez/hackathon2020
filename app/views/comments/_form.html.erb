<turbo-frame id="comment-form">
<div>
  <%= render partial: "comments/errors", locals: {comment: comment}%>
  <%= form_with(model: comment, url: validate_new_comments_path()) do |f| %>
    <%= f.label :body %>
    <%#= f.rich_text_area :body, class: "shadow-sm bg-white focus:ring-indigo-500 focus:border-indigo-500 mt-1 block w-full sm:text-sm border border-gray-300 rounded-md", 
      data: { action: "input->comment-form#validate" } %>

    <%= f.text_area :body, class: "shadow-sm bg-white focus:ring-indigo-500 focus:border-indigo-500 mt-1 block w-full sm:text-sm border border-gray-300 rounded-md", 
     oninput: "this.form.requestSubmit()" %>


    <%= f.hidden_field :filters %>
    <%= f.hidden_field :associated_model %>
    <%= f.submit class: "hidden" %>
    <%= f.button formaction: comments_path(), class: "rounded-lg px-4 py-2 bg-green-700 text-green-100 hover:bg-green-800 duration-300" %>
  <% end %>
  </div>
</turbo-frame>